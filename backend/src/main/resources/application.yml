server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://mysql.railway.internal:3306/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: oILqJYgEzJYjFslTRkSYbZImcsdNYvFC
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: SkillForgeJwtSecretKey2026SecureRandomString
    expiration-ms: 86400000
  openai:
    api-key: ${OPENAI_API_KEY:}
    model: llama-3.1-8b-instant

management:
  endpoints:
    web:
      exposure:
        include: health,info
